#include <bits/stdc++.h>
using namespace std;

int main()
{
    int l, c;
    cin >> l >> c;

    int m[l][c];

    for (int i = 0; i < l; i++)
    {
        for (int j = 0; j < c; j++)
        {
            cin >> m[i][j];
        }
    }
}